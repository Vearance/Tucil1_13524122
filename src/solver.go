package main

func Solve(b *Board, counter *int) bool {
	row := 0;
	queenPosition := make([]int, b.size)

	for i:=0; i < len(queenPosition); i++ {
		queenPosition[i] = -1  // queen does not exist
	}

	for row >= 0 {  // while syntax in Go
		
	}

	
}